# ─────────────────────────────────────────────────────────────────────────────
# CultivateNaturally — Backend Environment Variables
# Copy this file to .env and fill in your real values.
# NEVER commit .env to version control.
# ─────────────────────────────────────────────────────────────────────────────

# ── Server ───────────────────────────────────────────────────────────────────
PORT=3000

# ── MongoDB ──────────────────────────────────────────────────────────────────
# Get this from MongoDB Atlas → your cluster → Connect → Drivers
MONGO_URI=mongodb+srv://YOUR_USER:YOUR_PASSWORD@your-cluster.mongodb.net/cultivatenaturally?retryWrites=true&w=majority

# ── JWT Auth ─────────────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING

# ── Default Admin Credentials (created on first server start) ────────────────
ADMIN_EMAIL=admin@cultivatenaturally.shop
ADMIN_PASSWORD=CHANGE_THIS_STRONG_PASSWORD

# ── Stripe ───────────────────────────────────────────────────────────────────
# Live keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_YOUR_KEY_HERE
# Webhook secret from https://dashboard.stripe.com/webhooks
# Webhook URL: https://yourdomain.com/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE

# ── Frontend URL (used for Stripe success/cancel redirects) ──────────────────
# No trailing slash
FRONTEND_URL=https://cultivatenaturally.shop

# ── Email / Newsletter (SMTP) ─────────────────────────────────────────────────
# Used for sending newsletter campaigns via the CMS Marketing dashboard.
# Recommended free options: Brevo (300 emails/day), Mailgun, or Gmail App Password
# Gmail example: SMTP_HOST=smtp.gmail.com  SMTP_PORT=587  SMTP_USER=you@gmail.com
# Brevo example: SMTP_HOST=smtp-relay.brevo.com  SMTP_PORT=587  SMTP_USER=your-brevo-login
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=your_smtp_password_here
SMTP_FROM_NAME=Hamadryad
